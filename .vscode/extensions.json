{
  // The form is markdownlint's, so the editor that writes it is markdownlint's too. The rules are
  // .markdownlint-cli2.jsonc at the root, which this extension discovers on its own.
  "recommendations": ["DavidAnson.vscode-markdownlint"]
}
